name: OwnerGuard 1.0.38 Superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded release
        run: |
          echo "OwnerGuard 1.0.38 is superseded by the mandatory 1.0.39 owner-face enrollment release."
